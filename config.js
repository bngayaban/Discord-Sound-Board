const {token} = require('./token.json');
const prefix = '!sb';
const audioDirectory = './Audio/';
const timeoutTime = 5; //in minutes

module.exports = {
    token,
    prefix,
    audioDirectory,
    timeoutTime
};